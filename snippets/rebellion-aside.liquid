<aside class="rebellion-aside hidden lg:flex flex-col h-screen fixed left-0 top-0 pt-[80px] bg-surface-container-lowest border-r border-outline-variant w-64 z-40">
  <div class="p-8 border-b border-outline-variant">
    <div class="font-headline-lg-mobile text-primary font-bold uppercase">NODE_01</div>
    <div class="font-utility-label text-on-surface-variant mt-2">ACCESS_LEVEL: ELITE</div>
  </div>
  <div class="flex-1 py-4 aside-nav-links">
    <div class="aside-nav-item active flex items-center gap-4 px-8 py-4 bg-primary-container text-on-primary-container font-bold border-l-4 border-primary">
      <span class="material-symbols-outlined">grid_view</span>
      <span class="font-utility-label">DASHBOARD</span>
    </div>
    <div class="aside-nav-item flex items-center gap-4 px-8 py-4 text-on-surface-variant hover:bg-surface-variant transition-transform cursor-pointer">
      <span class="material-symbols-outlined">package_2</span>
      <span class="font-utility-label">INVENTORY</span>
    </div>
    <div class="aside-nav-item flex items-center gap-4 px-8 py-4 text-on-surface-variant hover:bg-surface-variant transition-transform cursor-pointer">
      <span class="material-symbols-outlined">sensors</span>
      <span class="font-utility-label">TRANSMISSIONS</span>
    </div>
    <div class="aside-nav-item flex items-center gap-4 px-8 py-4 text-on-surface-variant hover:bg-surface-variant transition-transform cursor-pointer">
      <span class="material-symbols-outlined">lock</span>
      <span class="font-utility-label">ENCRYPTION</span>
    </div>
  </div>
  <div class="p-8">
    <button class="w-full py-4 industrial-border font-utility-label uppercase hover:bg-white hover:text-black transition-all active:scale-95 duration-75">
      INITIALIZE_UPGRADE
    </button>
  </div>
</aside>

<style>
  .rebellion-aside {
    display: none;
    flex-direction: column;
    height: 100vh;
    position: fixed;
    left: 0;
    top: 0;
    padding-top: 80px;
    background-color: var(--color-surface-container-lowest);
    border-right: 1px solid var(--color-outline-variant);
    width: 256px;
    z-index: 40;
  }
  @media (min-width: 1024px) {
    .rebellion-aside {
      display: flex;
    }
  }
  .rebellion-aside .p-8 { padding: 32px; }
  .rebellion-aside .py-4 { padding-top: 16px; padding-bottom: 16px; }
  .rebellion-aside .px-8 { padding-left: 32px; padding-right: 32px; }
  .rebellion-aside .mt-2 { margin-top: 8px; }
  .rebellion-aside .flex-1 { flex: 1 1 0%; }
  .rebellion-aside .gap-4 { gap: 16px; }
  .rebellion-aside .border-b { border-bottom: 1px solid var(--color-outline-variant); }
  .rebellion-aside .font-bold { font-weight: bold; }
  .rebellion-aside .uppercase { text-transform: uppercase; }
  .rebellion-aside .w-full { width: 100%; }
  
  .aside-nav-item {
    display: flex;
    align-items: center;
    cursor: pointer;
    transition: transform 0.15s, background-color 0.15s;
  }
  .aside-nav-item:hover:not(.active) {
    background-color: var(--color-surface-variant);
    transform: skewX(-5deg);
  }
  .aside-nav-item.active {
    background-color: var(--color-primary-container);
    color: var(--color-on-primary-container);
    border-left: 4px solid var(--color-primary);
  }
  .aside-nav-item.active .font-utility-label {
    color: inherit;
  }
  
  .rebellion-aside button:hover {
    background-color: #fff;
    color: #000;
  }
  
  /* Update main content to offset for sidebar on desktop */
  @media (min-width: 1024px) {
    #MainContent, #header-group header-component, footer {
      padding-left: 256px;
    }
  }
</style>
